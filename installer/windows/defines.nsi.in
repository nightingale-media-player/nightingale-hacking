#filter substitution
!define BrandShortName        "@SB_APPNAME@"
!define BrandFullName         "@SB_APPNAME@"

!define BrandFullNameInternal "@SB_APPNAME@"
!define CompanyName           "POTI, Inc."
!define URLInfoAbout          "http://www.songbirdnest.com/"
!define URLUpdateInfo         "http://www.songbirdnest.com/"

!define AppVersion            "@SB_MILESTONE@"
!define AppOldVersion         "0.2.5"
!define GREVersion            "@SB_MOZILLA_VERSION@"
!define AppVersionWindows     "@SB_MILESTONE_WINDOWS@"

!define FileMainEXE           "Songbird.exe"
!define FileUninstallEXE      "Songbird-Uninstall.exe"
!define WindowClass           "SongbirdMessageWindow"
!define DDEApplication        "Songbird"
!define AppRegName            "Songbird"

!define ApplicationIni        "application.ini"

!define XULRunnerEXE          "xulrunner.exe"
!define XULRunnerDir          "xulrunner"

#if SB_USE_JEMALLOC==1
!define UsingJemalloc         "@SB_USE_JEMALLOC@"
#endif

#ifdef DEBUG
!define DebugBuild            "Debug Build"
!define MozCRuntime           "mozcrt19d.dll"
!define CRuntime              "msvcr80d.dll"
!define CPPRuntime            "msvcp80d.dll"
!define CRuntimeManifest      "Microsoft.VC80.CRT.manifest"
#else
!define DebugBuild            ""
!define MozCRuntime           "mozcrt19.dll"
!define CRuntime              "msvcr80.dll"
!define CPPRuntime            "msvcp80.dll"
!define CRuntimeManifest      "Microsoft.VC80.CRT.manifest"
#endif

#ifdef SB_FLASH_REDISTRIBUTABLE
!define InstallFlashRedistributable      "true"
!define FlashRedistributableInstaller    "@SB_FLASH_REDISTRIBUTABLE@"
!define FlashRedistributableInstallerEXE "flash_installer.exe"
#endif

#ifdef MEDIA_CORE_GST
!define IncludeLib "true"
!define IncludeGStreamerPlugins "true"
#endif

#ifdef SONGBIRD_OFFICIAL
!define PreferredInstallDir       "$PROGRAMFILES\${BrandShortName}\"
!define PreferredInstallerName    "${BrandShortName}_${AppVersion}_${ARCH}.exe"
!define PreferredIcon             songbird.ico
!define PreferredInstallerIcon    songbird_installer.ico
!define PreferredUninstallerIcon  songbird_installer.ico
!define VistaIcon                 songbird_vista.ico
!define IsOfficialBuild           "true"
#else
!define PreferredInstallDir       "$PROGRAMFILES\${BrandShortName}_${BUILD_ID}\"
!define PreferredInstallerName    "${BrandShortName}_${AppVersion}_${BUILD_ID}_${ARCH}.exe"
!define PreferredIcon             songbird_nightly.ico
!define PreferredInstallerIcon    songbird_nightly.ico
!define PreferredUninstallerIcon  songbird_nightly.ico
!define VistaIcon                 songbird_nightly.ico
#endif

!define ForceQuitMessage            "@SB_APPNAME@  is currently running and will be forced to quit."
!define BackupMessage               "Your existing media library and user profile will not be migrated to ${BrandShortName} ${AppVersion}.$\n$\nYou can continue to use your existing media library and user profile with your existing version of ${BrandShortName}.  In addition to installing ${BrandShortName} ${AppVersion}, would you like to keep your existing version of ${BrandShortName}?"
!define ShortBackupMessage          "Moving ${BrandShortName} ${AppOldVersion} to $INSTDIR\legacy\${AppOldVersion}\${BrandShortName}"
!define UninstallMessage            "We found an older version of ${BrandShortName} present on this computer, do you wish to uninstall it before installing ${BrandShortName} ${AppVersion}?"
!define UninstallMessageSameFolder  "The selected installation folder contains an older version of ${BrandShortName}. Do you wish to uninstall it and replace it with ${BrandShortName} ${AppVersion}?"
!define DirectoryNotEmptyMessage    "The folder selected for installation is not empty. $\n$\nYou must choose an empty folder to continue installation of ${BrandShortName}. "
!define AbortInstallMessage         "Are you sure you want to cancel installation of ${BrandShortName}?"
!define FlashRedistributableInstallMessage "Installing Adobe Flash Redistributable"
!define TakeSurveyMessage "We're sorry to see that you uninstalled Songbird. $\n$\nWill you consider helping us make Songbird better by taking a quick anonymous survey to help us improve the product?$\nIt shouldn't take more than 30 seconds to complete."
