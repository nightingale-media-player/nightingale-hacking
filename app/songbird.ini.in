#filter substitution
[steps:update]
0001=exec $APPDIR$cdriphelper.exe upgrade
0002=exec $APPDIR$sbAutoPlayUtil.exe -Upgrade

[steps:update:mac]
; any mac-specific steps go here; we don't have them at the moment.
