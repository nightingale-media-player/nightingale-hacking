#preedTODO: add copyright

DEPTH = ../../../..
topsrcdir = @top_srcdir@
srcdir = @srcdir@
VPATH = @srcdir@

include $(DEPTH)/build/autodefs.mk


IS_EXTENSION = 1

XPIDL_SRCS = IUnityProxy.idl \
             $(NULL)

# Causes circular dependency, so fix with headers and typelibs
# into this xpt (an xpt is like a compiled IDL read at runtime)
#XPIDL_MODULE = IUnityProxy.xpt \
#       $(NULL)

XPIDL_HEADERS = IUnityProxy.h \
                $(NULL)

XPIDL_TYPELIBS = IUnityProxy.xpt \
                 $(NULL)

## XXX: header and xpt files exported to the extension?

include $(topsrcdir)/build/rules.mk
